# Contributing

Work on one assigned task in `submission/<your-name>/<task-id>`. Add focused tests for every new success and error path. Do not edit tests belonging to another task, weaken validation, add persistent storage, or change dependency versions.

Run `pytest -q`, exercise the endpoint in `/docs`, and complete the pull request checklist. Students must not merge their own work.
